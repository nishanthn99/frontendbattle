import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'source.unsplash.com', 'picsum.photos', 'cdn.pixabay.com'],
  },
};

export default nextConfig;
